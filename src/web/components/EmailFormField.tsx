import FormField from "@/web/components/FormField"

const EmailFormField = () => {
  return (
    <FormField name="email" label="E-mail" type="email" autoComplete="email" />
  )
}

export default EmailFormField
